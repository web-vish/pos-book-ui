import EventsForm from "../../../components/EventsForms";

const CancelEvents = () => {
  return <EventsForm title="Cancel Events" pageType="Cancel" buttonText="Cancel Security"/>;
}

export default CancelEvents;
